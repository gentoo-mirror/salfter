#!/sbin/runscript
# Distributed under the terms of the GNU General Public License, v2 or later

VARDIR="/var/lib/yacoin"
CONFFILE="${VARDIR}/.yacoin/yacoin.conf"

depend() {
	need net
}

checkconfig() {
	if [[ "${YACOIN_USER}" == "" ]] ; then
		eerror "Please edit /etc/conf.d/yacoind"
		eerror "A user must be specified to run yacoind as that user."
		eerror "Modify USER to your needs (you may also add a group after a colon)"
		return 1
	fi
	if ! `getent passwd | cut -d ':' -f 1 | grep $( echo "${YACOIN_USER}" | cut -d ':' -f 1 ) -sq` ; then
		eerror "Please edit /etc/conf.d/yacoind"
		eerror "Specified user must exist!"
		return 1
	fi
	if `echo "${YACOIN_USER}" | grep ':' -sq` ; then
		if ! `cut -d ':' -f 1 /etc/group | grep $( echo "${YACOIN_USER}" | cut -d ':' -f 2 ) -sq` ; then
			eerror "Please edit /etc/conf.d/yacoind"
			eerror "Specified group must exist!"
			return 1
		fi
	fi
	if ! grep -q '^rpcpassword=' "${CONFFILE}"; then
		eerror "Please edit `readlink -f ${CONFFILE}`"
		eerror "There must be at least a line assigning rpcpassword=something-secure"
		return 1
	fi
	if ! stat -Lc '%a' "${CONFFILE}" | grep -q '^[4567]00$'; then
		eerror "`readlink -f ${CONFFILE}` should not be readable by other users"
		return 1
	fi
	return 0
}

start() {
	checkconfig || return 1
	ebegin "Starting Yacoin daemon"

	pkg-config openrc
	if [ $? = 0 ]; then
	   start_openrc
	else
	   start_baselayout
	fi
}

stop() {
       ebegin "Stopping Yacoin daemon"

       pkg-config openrc
       if [ $? = 0 ]; then
	   stop_openrc
       else
	   stop_baselayout
       fi
}

start_openrc() {
	start-stop-daemon \
	--start --user "${YACOIN_USER}" --name yacoind \
	--pidfile /var/run/yacoind.pid --make-pidfile \
	--env HOME="${VARDIR}" --exec /usr/bin/yacoind \
	--nicelevel "${NICELEVEL}" \
	--background \
	--wait 2000 \
	-- ${YACOIN_OPTS}
	eend $?
}

stop_openrc() {
	start-stop-daemon --stop --user "${YACOIN_USER}" \
	--name yacoind --pidfile /var/run/yacoind.pid \
	--wait 30000 \
	--progress
	eend $?
}

start_baselayout() {
	start-stop-daemon \
	--start --user "${YACOIN_USER}" --name yacoind \
	--pidfile /var/run/yacoind.pid --make-pidfile \
	--env HOME="${VARDIR}" --exec /usr/bin/yacoind \
	--chuid "${YACOIN_USER}" \
	--nicelevel "${NICELEVEL}" \
	--background \
	-- ${YACOIN_OPTS}
	eend $?
}

stop_baselayout() {
	start-stop-daemon \
	--stop \
	--user "${YACOIN_USER}" \
	--name yacoind \
	--pidfile /var/run/yacoind.pid
	eend $?
}
